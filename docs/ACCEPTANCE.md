# Scriber v0.1 验收概览

更新于 2026-09-18。**已按用户调整后的 Goal 范围交付。** 用户明确将下列三项转为后续自行验收，发现问题再提 bug，不再阻塞本次 Goal。此决定不是测试通过结论。本页按 SPEC 汇总证据，详细过程和限制见 [VALIDATION.md](VALIDATION.md)，操作见 [USAGE.md](USAGE.md)。

## 用户后续自行验收

| 项目 | 责任与状态 |
|---|---|
| 有线／USB、蓝牙耳机连接、断开与默认设备切换 | Yao 后续自行验收；受控物理切换尚未完成 |
| 长录音中麦克风重连原因与声音连续性 | Yao 后续自行验收；保留 36 次重连尝试及恢复警告，未证明全程无缺口 |
| 真实锁屏／解锁、睡眠／唤醒和合盖 | Yao 后续自行验收；真实受控转换尚未完成 |

以上项目使用中发现问题再提 bug；不继续等待当前协调问题的答复，也不自动重跑长测。功能要求保留，验收责任和时间按 [SPEC 第 7 节](../SPEC.md#7-验收清单) 调整。

## 当前安装版

- [v0.1.1 预览版](https://github.com/luyao618/scriber/releases/tag/v0.1.1) 已增加原生 macOS 图标，本机安装位置为 `/Applications/Scriber.app`。公开 ZIP 的下载哈希、安装后的全部文件和签名已核对；macOS 的原生图标读取显示新图标。
- 图标更新未修改录制代码。中英文启动、ZIP／DMG 内容一致性、单实例运行与历史／设置保留检查通过；没有重跑长测或将原有待验收项目标记为通过。详情见 [发布记录](RELEASING.md#已发布v011)。
- 以下保留 v0.1.0 的构建与验收来源。其中的旧本机构建／候选副本已在用户要求的清理中移入废纸篓，既有媒体和验证记录保留。

## v0.1.0 应用包证据

- 公开下载：[v0.1.0 预览版](https://github.com/luyao618/dayscribe/releases/tag/v0.1.0)，提供 Apple Silicon／macOS 26+ 的 DMG、ZIP 和校验文件，源码来自 `662ddb1b633c3f3901144c17038edda1f7dfeae4`。公开包使用 ad-hoc 签名、尚未 Apple 公证；首次打开可能需要在系统设置中手动放行。打包、解包／复制与语言启动检查通过，匿名下载文件与本地验证包逐字节一致；新签名下的首次放行及录制授权流程未实测。详情见 [发布说明](RELEASING.md)。
- 路径：`build/Scriber.app`，已更新为支持简体中文／英文的原生 Release 签名包。
- 启动时首选语言为英文则显示英文，否则默认简体中文；支持 macOS 单应用语言偏好，修改后重启生效。文件名、历史警告和保存目录不会因语言变化而改写。
- Sources 树：`0f499020c55bf13eda95c0ad75fabf1d76cfcfcf`；二进制 SHA256：`0f3c3c5db89fd9963dc265f04fa50bc0da37841417e137300b05ad1419f81cae`。构建输入逐文件哈希及复制包验证保存在 `artifacts/localization/final-package.json`。
- 本地化验证：109 项 Release 测试通过；六种独立进程语言配置、资源实际路径、词库完整性和签名检查通过。中英文主面板实际短录音各 1.9 秒及英文常用子页有原生操作证据；部分子页交互未完成本轮桌面复测，范围详见 [VALIDATION.md](VALIDATION.md#system-language-delivery-2026-09-18)。
- 现有 `build/candidate/Scriber.app` 与 `artifacts/localization/pre-change-Scriber.app` 保留为本地化前版本，不是当前默认包；视频素材来源和原始验证记录保留。

## 本地化前的交付与长测证据

- 当时路径：`build/Scriber.app`；保留同版本副本 `build/candidate/Scriber.app`。原生 Release 构建，已使用现有 Apple Development 身份签名并验证。
- 构建来源：`2a86beb94fff756650252cb645f169495f06d47d`，Sources 树 `b061be2af52f3cfe2cb33c41d4239170844c915d`，包含录屏目标不可用的中文提示。该版本用于下述既有交付证据；当前默认包已更新为上方本地化版本。
- 本次打包环境：Apple Silicon / arm64、macOS 26.7、Xcode 26.6（17F113）。应用最低要求仍为 macOS 26。
- 系统 LaunchServices 启动和权限检查通过；更新后的正常应用在隔离目录录下双路声音，AppKit 退出保存 **4.553104167 秒 / 218549 帧**。文件完整解码通过，再次启动恢复一条可用历史，个人历史未变。
- 本地证据：`artifacts/delivery-candidate-r3.json`、`video-target-message-validation-build.json`、`delivery-candidate-r3-smoke.log`、`delivery-candidate-lov6h_kk/verified.json`。候选二进制 SHA256：`240e6ac6cc2026eaf4d5d2932151aab257767575725c749c3540f2e00f79991a`。
- 八小时采集退出后，默认包已更新为同一签名二进制。默认路径再次通过正常启动、双路录制、AppKit 退出、完整解码和重启历史检查：**4.385833333 秒 /210520 帧**。旧长测包已原样归档。证据：`artifacts/delivery-default.json`、`delivery-default-smoke.log`、`delivery-candidate-nl3mp871/verified.json`。

八小时录音已完成，原长测包录下 **28800.16 秒**并保存。全文件原生／FFmpeg 解码、首尾测试音、原生跳转播放与资源复核通过；应用保留了麦克风设备变化后恢复的警告，原严格无警告检查失败。**这不是麦克风全程无缺口的证明。**

首次两小时录屏在约91分钟时因测试画面程序崩溃而中断并恢复，不算两小时通过。第二次完成 **7200.35425 秒**，保留文件的完整解码、配对音频及首尾同步复核通过，音画偏差中位数变化 **6.666 毫秒**。首次原生验证报错未复现，原因未定；原失败记录保留。两项长测的范围和限制详见 VALIDATION.md。

长测使用冻结的测试包；当时的交付与候选包包含之后的修正，均有短时正常生命周期验证。最新本地化包的验证另列于上方。这些版本不等同于同一二进制重新完成了长测；各版本来源均保留。

## 按需求检查

| SPEC 要求 | 当前证据 | 交付结论／后续安排 |
|---|---|---|
| 本机原生菜单栏应用，可构建、启动、呼出和收起 | 系统启动、原生菜单栏操作已验证；候选包启动与正常录制再验证通过 | 已交付，默认包生命周期检查通过 |
| 电脑声音、麦克风分别采集与混音，一个 M4A | 实际输入和可辨识信号、混音、静音段通过；八小时单文件完整解码及首尾信号通过 | 功能已交付；设备切换期间麦克风连续性由用户后续自行验收 |
| 开关来源、真实电平、最后一路保护 | 已验证真实 PCM、电平状态和原生开关；非工作来源不会显示正常收音 | 功能已交付；真实耳机转换由用户后续自行验收 |
| 区域、窗口、全屏录屏，MP4 有声音并另存 M4A | 三种范围均有真实采集和原生选择器记录；两小时窗口实录双文件解码及首尾同步复核通过 | 功能及原生操作已验证，已交付 |
| 时长、录制中改名、保存后配对改名 | 原生键盘、冲突编号、文件哈希和元数据验证通过；音频、录屏双文件及部分保存的实际警告／编辑／确认通过，已保存成员改名后字节一致 | 代码版本及默认包来源已核对，已交付 |
| 设置和记住两种目录；本次／下次目录明确 | 原生文件夹选择、取消、录制中更改及重启后写入验证通过 | 功能及原生操作已验证，已交付 |
| 历史搜索、播放、改名、Finder 定位 | 原生操作、实际播放输出、重启恢复及缺失文件状态已验证；中断录屏重启显示警告并可播放视频；部分保存默认播放可用音频，未完成视频预览禁用 | 已交付；两小时播放器补测为静音后台检查 |
| 模式和范围类型记忆 | 实际候选包按钮、两次进程重启及范围独立保存通过；保持待机、不自动开始，原设置已恢复 | 功能及原生操作已验证，已交付 |
| 全局快捷键与配置 | 后台唤起、修改、关闭、冲突保留和重启验证通过 | 功能及原生操作已验证，已交付 |
| 内置、有线／USB、蓝牙和断开／切换 | 内置及既有 Jabra USB 短采集已验证；有限重连和失败隔离有真实流测试 | 物理插拔、默认设备切换、蓝牙路径由用户后续自行验收 |
| 有界内存、八小时录音 | 实际28800.16秒、1382407680帧；全文件双解码、首尾信号、原生播放通过；峰值RSS85.094MiB，收尾0.077秒 | 原无警告检查失败已保留；36次重连及连续性评估由用户后续自行验收 |
| 两小时视频、首尾音画同步 | 第二次实际录制7200.35425秒；完整解码、211875个画面、两份音频345617004帧一致；最大绝对偏差18.333毫秒，首尾变化6.666毫秒；峰值RSS96.7MiB，收尾0.10秒 | 首次原生验证报错的未复现观察保留；测试包与交付包来源差异已记录 |
| 崩溃后恢复已写入内容 | 真实采集强制终止、原生恢复、重启不重复、原始字节保留和事务重试已验证 | 已交付，保留恢复范围限制，不承诺零丢失 |
| 磁盘不足、写入失败、部分成功 | 独立测试卷低空间停止／拒绝／恢复；真实写入失败和恢复已验证；实际部分保存标签、改名、历史和可用音频播放通过，原始未完成视频保留 | 代码版本及默认包来源已核对，已交付 |
| 防闲置睡眠、主动睡眠／合盖安全收尾 | 原生防睡眠断言持有／释放已验证；通知顺序有组件测试 | 真实睡眠、合盖及唤醒由用户后续自行验收；系统 30 秒期限仍是限制 |
| 锁屏状态真实，不虚报收音 | 已观察锁屏期间两项长测的实际帧数继续增长 | 受控锁屏／解锁转换及媒体检查由用户后续自行验收 |
| 与已认可原型一致、实际点击验证 | 原型基线保留；菜单、范围、文件、历史、快捷键以及模式记忆、三类保存警告均有实际操作／截图；录屏目标不可用已有中文提示和实测 | 已交付；物理设备及电源交互由用户后续自行验收 |
| 使用说明、源代码、构建脚本和测试记录 | 已提供，并核对当前控制名称、路径、权限和构建命令 | 已交付，按用户调整后的验收范围收尾 |

## 证据边界和交付检查

短测、生成的编码器样本、注入的通知和离屏渲染各有明确用途，不能作为真实长时、物理设备或桌面点击通过的证明。长期测试只有完成录制、解码和结果审查后才能标记通过。

收尾范围调整前，Goal 的 PR #5–#55 共 51 个均已合并，合并提交均在默认分支历史中。每个增量按个人身份、SSH 签名、自审和对应检查交付；原型自 PR #6 后未改动。旧 ASR PR #4／分支、原 stash 和 `.planning/scriber-v01/pre-goal-worktree` 备份仍保留。本次验收责任调整另以一个小文档 PR 交付。

默认包已更新，各类原生保存警告跟进已完成；源代码、可重复构建脚本、使用说明和两项真实长测的复核记录已交付。最终核对采用现有功能、原生操作、构建来源和默认包生命周期证据，没有在本次文档收尾中重新测试。用户已接手上述三项后续验收，因此本次 Goal 可按调整后的范围完成；原失败／警告及麦克风连续性限制随交付记录保留，不宣布设备、电源测试通过。
